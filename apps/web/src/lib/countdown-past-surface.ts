/**
 * Tratamiento visual alineado con `EventHeroBanner` cuando el objetivo ya pasó
 * (opacidad + escala de grises + contraste).
 */
export const COUNTDOWN_PAST_SURFACE_CLASS =
  "opacity-[0.88] grayscale saturate-0 contrast-[0.92]" as const;
